# dev-log 2026-05-26 — @fariZzzz — T4/T5 review cycle + off-trunk backlog to green

Covers the 5/25→5/26 review pass: the two in-flight trunk PRs (T4 [#34](https://github.com/Eliaaazzz/live-auction-system/pull/34), T5 [#38](https://github.com/Eliaaazzz/live-auction-system/pull/38)) and clearing the five stalled off-trunk PRs. Method throughout: **pull the PR, run it, challenge the new code paths with executable tests** — not review-by-reading. Several findings only showed up because the branch was actually built and run.

## T5 backpressure / multi-gateway (#38, doc #39 → merged)

Re-reviewed at `fe90763`. @Eliaaazzz had already shipped all my prior P1s (`sendBufFrames=256`, leading `done`-check post-close drop, `ws-envelope.md` "best-effort priority" + `AUCTION_EXTENDED`-is-critical). Rather than sign off on the diff, I pulled it and **challenged the fix itself**:

- **TC-T5-110** `TestT5ConcurrentCloseVsSendFloodIsRaceClean` — 16 senders × 500 `trySend`/`trySendLossy` racing `close()`×2. Pins the residual TOCTOU in the new leading `done`-check **and** the "send/lossy channels are never closed" invariant under `-race`. Clean.
- **TC-T5-111** `TestT5CatchupHeadroomBoundary` — `TestT5CatchupFitsInSendBuffer` only proves a *cold* buffer. The real guarantee is `sendBufFrames − catchupMaxGap` = **56** frames of headroom; at headroom the conn survives, at headroom+1 the replay's last push force-closes. So catchup-survives-replay is **conditional on concurrent fanout, not unconditional** — the T8-load residual @Eliaaazzz asked to relocate off the P1 line.

`go test -race ./...` green; multi-gateway fanout green against real Redis+MySQL. Probes landed in [#44](https://github.com/Eliaaazzz/live-auction-system/pull/44) (merged into #38); doc updated to v3.

**Verdict: the fix is solid.** Both P1s I raised are correctly closed.

## T4 persistence / evidence (#34, doc #35) — review→fix loop closed

#34 picked up a hardening commit (`8d8fa16`, @PDGGK) *after* my earlier approve, so I re-verified at the new head. Worth calling out: **that one commit resolved all five P0/P1 gap probes from my v1 #35 doc** —

| my v1 gap probe | `8d8fa16` fix |
|---|---|
| TC-T4-100 delete-detection | `TestT4HashChainDeletionBreaksAtSeq` |
| TC-T4-101 multi-worker `fillEventHash` wrong-prev | tx + `SELECT … FOR UPDATE` + `ErrPreviousEventHashMissing` |
| TC-T4-102 crash-between-INSERT-and-fill self-heal | `TestT4HashFillWaitsForPreviousHash` |
| TC-T4-104 empty-winnerId → infinite retry wedges worker | poison taxonomy (`ErrPermanentOrderProjection`/`ErrOrderProjectionMismatch`) advances the cursor |
| TC-T4-107 public `buyerId` | `/evidence` auth gate + `TestT4EvidenceRequiresAuth` |

I traced the now-non-atomic `projectSold` (status→SOLD, order, status→ORDER_CREATED): it self-heals across a crash in any window because re-projection is idempotent and the cursor only advances on success. `fillEventHash`'s `FOR UPDATE` lock order is descending-seq → deadlock-free. The hardening also moved the evidence card **off live Redis onto the persisted chain** via a pure `evidenceSummary()`.

- **TC-T4-110** `TestT4EvidenceSummaryDerivesFromChain` ([#45](https://github.com/Eliaaazzz/live-auction-system/pull/45), merged into #34) — the SOLD path was e2e-covered, but NO_BID / CANCELLED / LIVE-with-bids derivation + the order-override only run through that pure function. 5 table cases, all green.
- **Finding (P3, TC-T4-111):** the CANCELLED branch sets status but doesn't clear `winnerId`/`currentPriceCents`, so a cancelled-with-bids evidence card still shows a "winner" — which contradicts T3 **TC-T3-013** ("cancel → no winner"). Not a correctness bug; a presentation call (clear on cancel, or document as "last pre-cancel bid"). Flagged for product.

Re-affirmed APPROVE at the new head; doc updated to v2.

## Off-trunk backlog → all green (#16 / #18 / #24 / #25)

All four of my stalled PRs sat on `CHANGES_REQUESTED` from 5/23–5/24 that had **already been addressed by commits pushed after the review** but never re-reviewed (a recurring trap: author pushes the fix, reviewer never re-looks, the red state persists). Verified each by pull-and-test, then did the genuine remaining work and got CI green:

- **#18 observability** — the reviewer's "wiring never landed" was stale: `promtool` validates the stack (22 alerts + 21 recording rules), `--profile observability` resolves, `make up-obs` → `:3000`. The honest part is real: `/metrics` isn't wired in T1–T5, and `prometheus.yml` correctly comments out the lumen/ai-sidecar scrapes so they don't redden. Rebased onto main (resolved a one-line `.PHONY` Makefile conflict — kept both `up-obs` and `perf-smoke`).
- **#16 component docs** — Lua close/cancel contract was already synced to the impl (`6905e48`); I added honest "planned/not-wired" markers to the `/metrics` rows (06/09/13) and **fixed two real CI blockers**: gofmt on the contract-lint test, and a **leaked real DOUBAO endpoint id in `15-security.md`** that was tripping the `ep-[0-9]{8}` secret guard (redacted to `ep-<redacted>`).
- **#24 chaos-runner** — `seq_no_gap`'s secondary-event undercount is fixed (`ea02201`, verified end-to-end: `bidgen` records all seq-consuming event types, single bidder so no double-count). **New probe (P2):** because the invariant reconstructs the expected delta from *one bidder's observed* events, a connection-kill phase where the Timer hammers after the bidder drops would **false-red** even on a gapless auction — recommended verifying gaplessness straight from the Stream (`XRANGE … - +`, assert `1-0…N-0`) before partition phases land. gofmt + rebase.
- **#25 mentor runbook** — the AI-drill over-claim was softened (`42bf726`); I applied the same no-over-claim bar to two sibling rows the CR didn't name ("4 invariants pass/green" → "implemented + unit-tested; live-drill artifact pending").

## Cross-cutting / for the team

- **Secret in history (ops, not a code fix):** the real `DOUBAO_ENDPOINT_ID` was committed via #13's `.env.example`. `main` redacted it forward, but it's permanent in history — `15-security.md` already names the required action (rotate the endpoint). A branch rewrite won't unleak it.
- **Stale-base CI:** the three off-trunk branches were ~3 commits behind `main` and carried the pre-redaction `.env.example`; rebasing onto `main` fixed `guards` and picked up `gofmt`/test-base fixes. Rebasing review-stalled branches onto `main` before re-requesting is worth doing as a habit.
- **Two probes parked for later by design:** TC-T5-111 (catchup headroom under fanout) → T8 load; #24 Stream-`XRANGE` gapless check → before T9 partition phases.

## Evidence

- `go test -race -count=1 ./...` green against real Redis+MySQL (docker compose) at the T4 and T5 heads; `tools/chaos-runner` `go vet` + `-race` green; `tools/contract-lint` green.
- `promtool check config` + `check rules` clean on the #18 observability stack.
- No new deps. All four off-trunk PRs: `go` / `e2e` / `guards` green; re-requested review.
