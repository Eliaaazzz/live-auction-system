---
date: 2026-05-24
who: farizzz
trunk_step: T1 review-approve + PR #16/#18 patches + ack PR #20/#21/#22
related_prs: [#16, #18, #19, #20, #21]
related_issues: [#22]
---

## What changed

Five completed work items in one day:
1. **APPROVED PR #19 T1 trunk** after independent black-box re-run + line-by-line audit of 7 frozen contracts + [全员 approve] sign-off on new Lua return codes
2. **Patched PR #16** in `d9b9e09` — all 5 PDGGK CHANGES_REQUESTED items fixed (canonical Redis keys, anti-snipe stream-id design rethought, freeze/start don't consume seq, ROOM_SNAPSHOT shape, 7-state wording)
3. **Patched PR #18** in `1781f9c` — all 5 PDGGK items fixed (hammer p50/p99 + catchup panel + alerts, 500/50 visibility, T1-safe single-target scrape, persistence naming, Lua p99<5ms alert)
4. **Approved PR #20** (PDGGK dev-log) + **PR #21** (diagrams v4) — both substantive acks not vanilla LGTM
5. **+1 on issue #22** PR-review checklist with one ≤20-token extension proposal (explicit verdict line)

## How

**PR #19 review: black-box first, code-read second.** Cloned `elia/T1-dummy-bid-roundtrip` branch, ran `make up && make seed && make e2e-dummy-bid && make verify` on macOS Docker — all passed (E2E_AUCTION_ID=auc_521c94a4d72d4047, verifier `consistent: stream=1 mysql=1`). Then read Lua scripts + api.go + ws.go + ci.yml to verify the contracts I'm gate on:
- 7 frozen invariants from V9 §0 / RFC v2 §0 individually checked and located in code (TYPE guards at `place_bid.lua:8-17`; Stream ID at `:55`; Redis TIME at `:38-40`; dedupe behavior at `:25-29`; single-seq writer only in place_bid; Redis-down→ERR_AUCTION_PAUSED at `ws.go:219-227`)
- 6 §8 security baseline controls (EnableDevLogin flag, JWT_SECRET startup-fail, server-side ownership, facts confirmation gate, direct ack to sender, Origin allowlist)
- All 18 codes in `proto/error-codes.md` matched against my #17 co-design proposal — exact match

**Approve with 2 non-blocking T2 follow-ups** instead of either "rubber-stamp APPROVE" or "CHANGES_REQUESTED for nitpicks":
- `UpdateAuctionStatus` MySQL-after-Lua reverse drift window (Eliaaazzz already self-flagged + fixed surfacing in `5c1c94d`; the underlying race remains; documented for T2)
- `bidErrCode` string-match on "NOSCRIPT" is fragile if go-redis changes error wrapping; documented for T2

This is the right granularity — block-or-rubber-stamp would have either delayed mentor demo or signed off on hidden tech debt.

**PR #16 patches: 3 of 5 were real design rethinks, not text edits.**
- The anti-snipe `<seq>-1` synthetic suffix I'd documented violated V9 §0 boundary 3. **Rethought:** drop synthetic suffix entirely, encode extension as `extended:true` + `newEndAtMs` payload fields inside the single BID_ACCEPTED Stream entry. Client seq-guard simplifies to dedupe-by-seq-alone. FE synthesizes the UI "extended" toast from the flag. **New design that's both simpler AND honors the frozen boundary** — net win, not just a text patch. (See `03-lua-scripts.md` §`place_bid.lua` notes.)
- freeze/start consuming seq → don't consume. Updated pseudocode + added cross-ref to PR #19 `freeze_rules.lua:12-15` for grounding.
- ROOM_SNAPSHOT_REDIRECT → full ROOM_SNAPSHOT envelope, shared shape with REST snapshot per V9 §6 "shared `$ref`".

**PR #18 patches: 3 of 5 were "I had it wrong; fix the design".**
- My v1 prometheus.yml scraped `lumen-gateway-2:8081` and 4 other per-mode hostnames that don't exist in T1 single-process (`--mode=all`) topology. Would have shown GatewayDown red on demo. **Fix:** collapsed to single `lumen:8080` target by default; T5+ multi-process targets commented out with un-comment instructions.
- 500/50 SLO visibility — I had connection count but no target line, no active-bidder count. **Fix:** restructured Row 1 to show target thresholds (green at 500 / green at 50) so demo viewers see at-a-glance whether load test is passing.
- Lua p99 < 5ms hot-path budget alert missing. **Fix:** added LuaScriptBudgetBreached + Critical alerts. This was an *explicit V9 §4.2 gate I had missed*.

**PR #20 ack with stylistic input (not vanilla LGTM).** PDGGK's dev-log has a `mostly Chinese + English technical terms` voice. I asked which tone the team is converging on so my Day 0+1 (this PR after merge) matches. Small but real co-design question.

**PR #21 ack with cross-link suggestion.** Noted diagram #8 ("Delivery Ownership and Evidence Map v4") explicitly puts Chaos Runner in Operator/QA column under "five fault videos" → my upcoming T9 chaos-runner work has a clear evidence target. Offered to embed his `v4/*.png` URLs into my component docs `00-overview.md` + `14-chaos.md` (chaos drill state flow) as a follow-up PR.

**Issue #22 +1 with a 1-line addition.** Suggested adding step 7: `Approve / Request Changes / Block — say which (and why one-line) in the review body`. Per #15 §3 the verdict needs to be explicit. Doesn't grow the ≤100-token budget meaningfully.

## AI prompt summary

- Claude Code for the 5 items.
- For PR #19 review: read the actual code with the explicit "verify this matches frozen contracts" mode. Cross-referenced against V9 §0 boundary numbers + RFC v2 §0 + PR #13 ws-protocol.md. Did NOT do "summarize what the PR does" prompts (Copilot already did that, no value to repeat).
- For PR #16 patches: spent ~15 min thinking about the anti-snipe redesign before writing — wanted the new design to be *better* than the original `<seq>-1` split, not just compliant. Outcome: single Stream entry + payload flag is genuinely simpler.

## Decision points

- **Approve PR #19 with non-blocking T2 flags, or hold for fixes today?** Chose approve. Mentor sync is tomorrow. The 2 flagged issues are real but T2-bucket-acceptable (Eliaaazzz already fixed the user-visible MySQL error surfacing; the residual race is documented). Blocking would delay merge past mentor sync window.
- **For PR #16 anti-snipe redesign: minimal patch or full rethink?** Chose full rethink. Minimal patch (e.g. "moved the AUCTION_EXTENDED Stream entry into next-seq slot") would have introduced a new boundary violation. Single-entry-with-flag design solves both the V9 §0 b3 violation AND the client seq-guard complexity.
- **PR #18 single-gateway-safe alerts: rewrite vs profile-gate?** Chose profile-gate (comment-out with un-comment instructions). Rewriting would lose the T5+ topology spec; profile-gating preserves it for future use while making T1 alerts clean.
- **Day 0+1 dev-log: ship as separate PR or include in any existing branch?** Chose separate PR `farizzz/dev-log` after PR #20 merged. Atomic. Per-day cadence visible.

## Tests / evidence

Artifacts:
- https://github.com/Eliaaazzz/live-auction-system/pull/19#pullrequestreview (APPROVE for T1 trunk)
- https://github.com/Eliaaazzz/live-auction-system/pull/16/commits/d9b9e09 + https://github.com/Eliaaazzz/live-auction-system/pull/16#issuecomment-4525685575 (PR #16 5-finding response)
- https://github.com/Eliaaazzz/live-auction-system/pull/18/commits/1781f9c + https://github.com/Eliaaazzz/live-auction-system/pull/18#issuecomment-4525699981 (PR #18 5-finding response)
- https://github.com/Eliaaazzz/live-auction-system/pull/20#pullrequestreview (APPROVE dev-log)
- https://github.com/Eliaaazzz/live-auction-system/pull/21#pullrequestreview (APPROVE diagrams v4)
- https://github.com/Eliaaazzz/live-auction-system/issues/22#issuecomment-4525702272 (+1 with extension)

Independent re-run on macOS Docker: `make up` (4 containers Healthy <60s) → `make seed` → `make e2e-dummy-bid` (PASS) → `make verify` (`consistent: stream=1 mysql=1`). Mentor 5/25 demo path verified independently.

## Open questions

- **Mentor sync content**: Eliaaazzz needs to present "T0 closed + T1 stub demo" with risk register; what's my speaking role (if any)? Will follow leader's call.
- **PR #16/#18 re-review**: PDGGK has finite cycles; if he can't re-review before mentor sync, do my patches land without his second look? Both PRs are docs/config only, low risk to merge — but Workflow v2 §3 says reviewer-blocking. Probably best: leave both PRs OPEN through mentor sync, merge after his ack.
- **T9 chaos-runner taxonomy**: my proposed 3 additional phases (slow-client, schrödinger slow-redis, hash-chain-tamper) are diversification beyond PDGGK's diagram #5 baseline. Will the team accept them or rate them scope creep? Test = how PR for chaos-runner skeleton is received.

## Next step

- Ship Day 0+1+2 dev-log entries (this file series) as PR `farizzz/dev-log`.
- Start `tools/chaos-runner/` skeleton on `farizzz/chaos-runner-skeleton`. T9 territory, no T1 conflict. Bake in my proposed phase taxonomy + invariants as discussion seed, not silent expansion.
- Watch for: Eliaaazzz's T1 merge (PR #19 unblocked after my approval); PDGGK's re-review on my patches; PR #13 merge (still open, V9 SoT-blocking).

## Reflection — diversification scorecard for the week

| Lens action | Diversification angle | Outcome |
|---|---|---|
| Self-critique on my own #14 v1 | 13 challenges including 4 BLOCKING | Eliaaazzz ratified 11 of 13; 2 corrected back at me |
| Pre-design Lua return codes on #17 | Compress T0a authoring time | Matched PR #19 implementation exactly |
| PR #18 ahead-of-trunk parallel | Demo line "可观测性" pre-loaded | Mergeable as-is post PDGGK fixes |
| PR #16 anti-snipe redesign | New simpler design honors frozen boundary | Patched in `d9b9e09` |
| PR #19 review with T2 follow-ups (not block) | Honest but not blocking | Unblocked mentor demo |
| Issue #22 ≤20-token extension | Add explicit verdict line | Pending team reaction |

Workflow v2 §3 "reviewer challenges, not approves" working as intended. Net: 11 challenges adopted, 2 walked back, 1 design fully rethought (anti-snipe), 3 follow-up patches that improved on prior text. Not 13/13 — 11/13 — and that gap is the diversification healthy signal.
