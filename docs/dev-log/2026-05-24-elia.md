# dev-log 2026-05-24 — @Eliaaazzz (leader / trunk)

Per [#15](https://github.com/Eliaaazzz/live-auction-system/issues/15) Workflow v2. Trunk step: **T2 atomic bid core** (V9 §10). Branch `elia/T2-atomic-bid-core`, stacked on `elia/T1-dummy-bid-roundtrip` (PR #19 still open) so T2 reviews on its own diff; retarget to `main` once T1 merges. `[all-member approve: Lua return]` — the `place_bid` return shape is on the §6 coupling surface.

---

## Entry 1 — T2 implementation

**What changed** — turned T1's accept-any `place_bid.lua` into the full atomic bid core (V9 §10 T2):

1. **`place_bid.lua` — atomic state/amount/cap/dedupe/seq/anti-snipe/Stream/publish.**
   - Amount validation: reject `amount < currentPrice + increment` **or** `amount > cap` (single `ERR_TOO_LOW` namespace; `cap==0` = no ceiling).
   - **Anti-snipe** (`OK_EXTENDED`): bid within `extendWindowSec` of `endAtMs` bumps `endAtMs += extendSec*1000`, `HINCRBY extendCount`, emits `AUCTION_EXTENDED` — same script, no `extend.lua`.
   - **Cap-hit / buy-now** (`OK_SOLD`): `amount >= cap` → `status=SOLD` + `AUCTION_SOLD`. Cap-hit beats anti-snipe.
   - Secondary events **consume their own seq** (`seq+1`) so every Stream entry keeps a unique `<seq>-0` id and the log stays gap-free.
   - Leaderboard via `ZADD GT` (each member's accepted max).
2. **Typed Pub/Sub fanout** — Lua publishes `{type,seq,data}` (`PubMessage`); the gateway subscriber re-emits the right wire type (`BID_ACCEPTED`/`AUCTION_EXTENDED`/`AUCTION_SOLD`). One channel, N event types; Stream stays the source of truth.
3. **Client `seq-guard`** — `apps/lumen/internal/seqguard` (drop duplicate ack-vs-broadcast + out-of-order frames; `Gap()` for the T5 snapshot fallback). Mirrored in `web/room.html`, which now also renders the leaderboard, a countdown, and the extension/SOLD events.
4. **Leaderboard read** — `store.Leaderboard` + `GET /api/auctions/{id}/leaderboard?n=` (token-gated; money as string; n∈[1,100]).
5. **perf-smoke** — `lumen perf-smoke` + `make perf-smoke` + compose service: pipelined bids measuring ack/broadcast p50/95/99 vs §4.2 **fallback** budgets (ack<200ms, broadcast<500ms). Floor-check only (single gateway, no fixed box) — not the 500/50 SLO gate (T8).
6. **Contracts** — materialized the T2 `place_bid` return shape + `PubMessage` in `redis-keys.md`; `OK_EXTENDED`/`OK_SOLD` in `error-codes.md`; `AUCTION_EXTENDED`/`AUCTION_SOLD` data shapes + seq-guard + leaderboard in `ws-envelope.md`.
7. **CI** — added a `redis:7` service to the `go` job + `go test -race`, so the §4.1 / Lua-return-code suite runs as a real gate (not skipped). Added perf-smoke to the e2e job.

**How / decisions**
- **Lua hot path stays the only writer**; Go's `PlaceBid` just maps the return array. Anti-snipe/cap are branches inside the one script (no rollback risk).
- **Two events per extend/cap bid** (BID_ACCEPTED then AUCTION_EXTENDED/SOLD), each with its own seq — chosen over reusing one seq so `<seq>-0` stays unique and catchup/seq-guard see a contiguous log.
- **seq-guard is Go-first** (CI-gated pure logic, 100% covered) with a JS mirror for the demo client — the repo has no JS test runner yet, so the Go impl is the spec.
- **perf-smoke is a floor-check, deliberately**: green ≠ de-risked 500/50 (§4.2 note). CI uses the fallback budgets to stay non-flaky on shared runners.

**Tests / evidence** — verified locally **and** in a `golang:1.22` + linked-redis container reproducing the CI `go` job:
- `go test -race ./...` green (redis-backed Lua suite included); `tidy`/`gofmt`/`vet`/`build` clean.
- §4.1 / Lua harness (real Redis): seq monotonic **no gap under concurrency** (distinct-amount stampede, same-amount single-winner, **contended ladder → N accepts, seq 1..N**), same `client_bid_id` → **byte-identical** original ack (no mutation), every return code (`OK_ACCEPTED/EXTENDED/SOLD`, `DUPLICATE`, `ERR_NOT_LIVE/AFTER_END/TOO_LOW(×2)/AUCTION_PAUSED/INTERNAL`), anti-snipe `endAtMs`↑ + `AUCTION_EXTENDED` in stream, cap-hit `OK_SOLD` + post-SOLD `ERR_NOT_LIVE`, Pub/Sub payloads carry the right type+seq.
- Coverage: model 100%, seqguard 100%, auth 100%, store hot path covered (storedb MySQL methods + server stack code are covered by the e2e job, not unit). Global unit % is dragged by stack-only code — the real T2 correctness gate is the Lua suite + e2e, per §9's "package floor + named-suite" framing (the global-% gate lands with #14 tooling).
- Full stack (`make up` healthy): `e2e-dummy-bid` **PASS**, `verify` **consistent**, `perf-smoke` **PASS** (ack p95≈281µs, broadcast p95≈324µs, 200/200/200). Live checks: leaderboard **401 without token / 200 with**; create-auction `increment=0` → **400**.

---

## Entry 2 — multi-angle code review (3 parallel reviewers) + fixes

Ran code-reviewer / security-reviewer / go-reviewer in parallel (correctness · perf · design · SE-principles · security · API/plan-compat). Fixed the valid findings:

- **[CRITICAL] leaderboard unauthenticated** → token-gated (the bidder list is more sensitive than the single current price `GET /auctions/{id}` already exposes); `room.html` sends its token.
- **[HIGH] `displayName` was the userID** (T1 dev-log had deferred this to T2) → resolve the nickname once at WS connect (`store.UserNickname`, falls back to id) and pass it to `place_bid`.
- **[HIGH] perf.go data race** on the broadcast-latency slice → guarded by mutex, copied under lock; observer goroutine bounded. Confirmed clean under `go test -race`.
- **[HIGH] `BidAcceptedData` dropped `serverTimeMs`** on the direct-ack re-serialisation → added the field so the direct ack and the Pub/Sub broadcast carry the same shape.
- **[HIGH] increment==0 / unwinnable rules** → `model.Rules.Validate()` (increment>0, duration>0, cap≥start+increment unless 0) enforced at create-auction (400).
- **[MEDIUM] store.PlaceBid short-array panic** → bounds-check the Lua result before indexing.
- **[deferred, documented] per-connection bid rate limit** (`ERR_RATE_LIMITED`) → TODO at the dispatch site; it's an all-member-approve wire-code change and dedupe already makes retries cheap (revisit before multi-gateway T5).
- WS frame read-limit (32 KiB) + `clientBidId` length cap (128) added (§8 hardening). Strengthened tests: pub seq assertions, robust after-end timing, contended-ladder no-gap.

**Open questions** — `[all-member approve]` sign-off for the T2 `place_bid` return shape (`OK_EXTENDED`/`OK_SOLD` + secondary-seq) from @PDGGK + @fariZzzz. Rate-limit wire code (`ERR_RATE_LIMITED`) needs an all-member contract PR before T3/T5.

**Next step** — push, open PR linking the T2 scope; @fariZzzz to independently re-run the §4.1 suite + perf-smoke (their blocking gate).

---

## Entry 3 — address PR #26 review (@fariZzzz APPROVED, 5 findings)

@fariZzzz approved with 5 findings (none merge-blocking) + perf methodology questions. Addressed:

- **[HIGH #1] runaway anti-snipe** — `extendCount` was tracked but never enforced; two bidders could extend forever. Added **`maxExtensions`** to the rule DSL (`0` = unlimited, back-compat): `Rules.MaxExtensions` + `Validate`, `auction_rules.max_extensions` column, `CreateAuction`/`GetRules`, `freeze_rules.lua` copy, and `place_bid.lua` gate (`extendCount < maxExtensions`). Past the cap an in-window bid is a normal `OK_ACCEPTED` (no bump, no `AUCTION_EXTENDED`). Seed `auc_demo` set to 5. Test: `TestPlaceBidAntiSnipeRespectsMaxExtensions`.
- **[MED #3] DUPLICATE-after-extend** — added `TestPlaceBidDuplicateAfterExtend` proving the retry replays the cached ack (which **already carries the extended `endAtMs`** — so it is *not* stale, slightly better than the finding implied) with no re-extend / no new event; missed `AUCTION_EXTENDED` recovery is reconnect+catchup only. Documented in `ws-envelope.md`.
- **[#4 displayName / #5 cap==0]** — documented as known semantics/limitations in `ws-envelope.md` (connect-time nickname caching; `cap==0` = no buy-now + UI default-value caution → T10).
- **#2** (anti-snipe single- vs double-Stream-entry) — @fariZzzz conceded T2's double-entry design; no change.
- **Perf methodology Q** — answered on the PR: `ack` = `time.Now()` immediately before `bidder.WriteJSON` → matching `BID_ACCEPTED` read (full WS round-trip, client-side); `broadcast` = same send instant → **observer** `ReadJSON` (Lua adjudication + PUBLISH + fanout + observer receive, not fanout-only); single in-flight bid, 1 bidder + 1 observer → no contention by design (floor-check, not the 500/50 T8 gate). µs numbers are localhost-docker round-trips.

**Tests / evidence** — `go test -race` green in the CI-replica container; full fresh stack (`down -v` → rebuild, exercises the new `max_extensions` column) `e2e-dummy-bid` PASS / `perf-smoke` PASS / `verify` consistent; `auc_demo` Redis state shows `maxExtensions=5`.

**Open** — `maxExtensions` is an additive rule-DSL field on the `[all-member approve]` surface; @fariZzzz requested it, @PDGGK to ack. `ERR_RATE_LIMITED` still the T3 carry-over.

**Next step** — push to `elia/T2-atomic-bid-core`, reply to @fariZzzz point-by-point.

---

## Entry 4 — address PDGGK review (#26 CHANGES_REQUESTED) + merge reviewer test PRs #27/#28

@PDGGK CHANGES_REQUESTED (6 findings) + a deeper review doc. Resolved conflicts with the **Keep-#1** state-machine decision (verified #1↔#2 genuinely disagree: #2 RFC says `BIDDING/HAMMERED/PASSED/RESERVE_NOT_MET`; #1 explicitly *corrected* those to `LIVE/SOLD/NO_BID` — so #1 governs and a contract-pin note goes to #2; **reserve stays P1**).

**Fixed**
- **H4/F7 seller self-bid** — `sellerId` frozen into Redis state; `place_bid.lua` rejects `userId==sellerId` → `ERR_NOT_ALLOWED` (hot path, no MySQL hit).
- **H2 money precision** — `MaxMoneyCents = 2^53-1` enforced at gateway + `Rules.Validate` + Lua ceiling; money now stored as **exact decimal strings** (HMSET/ZADD), since a Lua number arg formats via `%.14g` and loses precision past 14 digits.
- **H3 critical WS drop** — `Conn` uses a `done` channel + `sync.Once` close (fixes the latent send-on-closed race too); a full send buffer now **drops the connection** (client reconnects + re-snapshots) instead of silently losing a critical event; `room.html` auto-reconnects.
- **H1 schema drift** — idempotent startup migration adds `auction_rules.max_extensions` on pre-existing volumes. Verified live: dropped the column → restarted lumen → migration re-added it.
- **M1 dirty-write** — `place_bid.lua` preflights stream-vs-state seq lockstep before any mutation → clean `ERR_INTERNAL('seq_stream_mismatch')`, no dirty write.
- **M2 seqguard** — `Gap` no longer overflows at `math.MaxInt64` (dup → 0, not −1).
- **F2 cap** — cap-aware `required = min(current+increment, cap)` so a buy-now reaches the cap when the increment overshoots; over-cap still `ERR_TOO_LOW` (per #1); `Rules.Validate` relaxed to `cap>start`.
- **F10** — `Cents.Scan` errors on bad bytes; `serverTimeMs` documented in `ws-envelope.md`.

**Dropped / deferred (with reason)**
- **F3 idempotency-conflict-as-error** — would violate #1 §4.1 ("retry returns original ack, **not** DUPLICATE-as-error") *and* PDGGK's own `dedupe_ttl` test. Kept DUPLICATE-replay (idempotency key is authoritative).
- **F4 persistence Stream-first** → T4 (Persistence+order+evidence). **F5/F6 gateway-Stream-verify + `lastSeq` catchup** → T5 (the defining T5 work; docs already say "catchup is T5", and the new WS close-on-slow gives correct recovery via reconnect+snapshot in the meantime).

**Merged reviewer test PRs** (kept authorship): **#27** (@fariZzzz, 8 tests) + **#28** (@PDGGK, 9 tests). Reconciled 3 to the chosen fixes: #28 `money_precision` → option-a boundary; #28 `XADD`-conflict → clean `ERR_INTERNAL`; #27 `NoExtensionCap` tripwire → refreshed to assert the `maxExtensions` cap.

**Tests / evidence** — `go test -race ./...` green (model/seqguard/server/store, incl. all merged suites) in the CI-replica container; full stack `e2e-dummy-bid` PASS / `perf-smoke` PASS (ack p95≈313µs, broadcast p95≈352µs) / `verify` consistent; H1 migration re-add verified live; PDGGK facts-gate hidden test PASS against the running stack.

**Next step** — push, reply to @PDGGK + @fariZzzz point-by-point; await all-member sign-off on the `maxExtensions` / `sellerId` rule-DSL additions ([all-member approve] surface).

---

## Entry 5 — best-practice pass: every hidden test in CI, nothing skipped

Decision (leader): go best-practice, no time pressure — wire **all** hidden tests (both teammates' #27/#28 + my review-doc HT-001…HT-092) into CI and **build the deferred features** so they pass for real, and ensure **no test is skipped** in CI (provision the infra).

**Features built (were deferred):**
- **F4 persistence Stream-first** — worker does an initial + periodic Stream sweep (SCAN + XRANGE from cursor); Pub/Sub is only a wakeup. Survives missed/lost hints, late start, restart. `InsertEvent` flags a different-payload-for-same-seq (`ErrEventPayloadMismatch`).
- **F5 Stream-authoritative broadcast** — gateway reads the canonical Stream on a Pub/Sub hint and fans out; a forged Pub/Sub event not in the Stream is never broadcast. `decodePub` removed.
- **F6 `lastSeq` catchup** — `ROOM_JOIN{lastSeq}` replays the missed Stream delta then snapshot; gap > 200 → snapshot fallback. Reconnect resyncs (with H3 close-on-slow).
- **schemaVersion** on every envelope (HT-002); **Cents.Scan** errors on bad bytes; **config** fail-fast test; **leaderboard-n** clamp extracted + tested.

**CI / no-skip:** the `go` job now runs **Redis + MySQL** services (+ schema load + `MYSQL_DSN`); the test step **fails if any test reports `--- SKIP`**. An in-process server harness (httptest + real store + hub + worker) runs the REST/WS hidden tests (facts-gate, seller-WS, forged-Pub/Sub, catchup) without an external stack — so they execute in CI rather than skip.

**Idempotency (F3) — deliberately NOT a conflict error:** my review-doc HT-021 wanted same-key-different-amount → conflict, but #1 §4.1 and both teammates' tests mandate DUPLICATE-replay; overriding a frozen contract isn't best-practice, so replay stays (HT-021's scenario is covered asserting replay + no mutation). Conflict-detection noted as a future contract PR.

**Tests / evidence** — full `go test -race` green with **zero skips**, verified in a CI-replica container (golang:1.22 + linked redis + mysql + schema). Full stack re-verified after F5/F6: `e2e` PASS (observer broadcast via Stream fanout), `perf-smoke` PASS (ack p95≈0.5ms, broadcast p95≈0.8ms — the Stream read adds ~0.5ms, still ≫ under the 500ms budget). Contracts updated (ws-envelope schemaVersion/catchup/Stream-authoritative; redis-keys pub-is-wakeup).

**Open** — `schemaVersion` envelope field + Stream-authoritative fanout are [all-member approve] surfaces; multi-gateway fanout + backpressure channel split remain T5.

---

## Entry 6 — final-review blocker: unwinnable no-cap auction (fixed)

Final reviewer found a real new edge: `Rules.Validate()` only checked each money field `<= MaxMoneyCents`, not that a **no-cap** auction's first required bid `startPriceCents + incrementCents` is reachable. So `{start: MaxMoneyCents, increment: 1, cap: 0}` was *created* successfully but **unwinnable** — the gateway/Lua reject any `amount > MaxMoneyCents`, yet the first required bid is `MaxMoneyCents + 1`.

**Fix** — one guard in `Rules.Validate()` after the money-upper-bound case (subtraction form avoids overflow): reject `capPriceCents == 0 && startPriceCents > MaxMoneyCents - incrementCents`. Cap-aware clamping stays valid (a cap below the first increment is still allowed — the required price clamps to the cap).

**Tests** (from the reviewer's patch, applied): `model` reject + cap-clamp-allowed, `server` REST create-auction → 400, `store` Lua proves a bypassed rule still rejects with no seq/stream/state side effects. The model + REST tests were red on the prior HEAD, green after the guard.

**Evidence** — `go test -race ./...` green, zero skips, in a CI-replica container (golang:1.22 + redis + mysql + schema). Contract note added to `redis-keys.md` (creation guard).
