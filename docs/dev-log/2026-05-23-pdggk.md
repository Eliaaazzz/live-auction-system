# Dev-log — 2026-05-23 — @PDGGK

> Adversarial PR review day. All three trunk PRs — #19 / #16 / #18 — got CHANGES_REQUESTED.

## What changed

- Ran an adversarial review over 3 new PRs: **PR #19** (Eliaaazzz, T1 trunk dummy bid roundtrip) + **PR #16** (fariZzzz, 17-component breakdown) + **PR #18** (fariZzzz, observability scaffold)
- All 3 review drafts were formally posted to GitHub via `gh pr review --request-changes --body-file` (a formal review state, so it affects mergeability)
- Pinged Eliaaazzz for a re-review on PR #13 (stuck for 30+ hours since the 5.21 13:51 CHANGES_REQUESTED, while we had pushed 5 commits addressing all 11 findings plus the V9 drift alignment)
- Kicked off the dev-log discipline (this PR, `pdggk/dev-log`) — a reference implementation of V9 §5 + #15 §5 before the 5/25 mentor sync

## How

- Codex with profile=readonly (1 run × 3 subagents × 3 PR reviews) — it touches no code, PRs, or docs
- Because `readonly` blocks file writes, all 3 drafts were inlined in `report.md` and extracted into separate files with `awk`
- Used `gh pr review --request-changes` rather than a plain comment (a GitHub formal review state, which triggers reviewable status and auto-notification)
- The isolation grep is clean across every review draft (no codex-pm / codex-supervisor / handoff/runs strings leaked into PR comments)

## AI prompt summary

- "Adversarially review 3 PRs through PDGGK's product/demo lens, against the #15 §8 standard that 'a reviewer must challenge, not just approve', with ≥ 3 findings per PR"
- Every subagent must check: the 9 V9 engineering boundaries / Lua naming (place_bid / close_auction / cancel_auction / start_auction) / the canonical 7-state machine / WS SCREAMING_SNAKE / the money string boundary / dev-log existence / **mentor 5/25 demo readiness**
- Assessed on 4 dimensions: APPROVE / CHANGES_REQUESTED / COMMENT / mentor readiness

## Decision points

- **Post CHANGES_REQUESTED on all 3 PRs at once** (not just #19): one clear signal to the team, with the timeline in sync
- **A formal review beats a plain comment**: it affects mergeable status and triggers a GitHub auto-notification to the author
- **Keep file:line refs in the drafts**: the reviewer can jump straight there (e.g. `apps/lumen/internal/lua/place_bid.lua:27`)

## Tests / evidence

- PR #19: 5 findings (🟠 money string boundary / 🟠 facts confirm not enforced / 🟠 Lua has no type guard / 🟡 lossy Pub/Sub ack / 🟡 printed URLs 404)
- PR #16: 5 findings (🟠 Redis key drift / 🟠 anti-snipe violates `<seq>-0` / 🟠 freeze/start reintroduce a seq gap / 🟡 ROOM_SNAPSHOT_REDIRECT / 🟡 the "5-state machine" mislabel)
- PR #18: 5 findings (🟠 V9 §4.3 metric incomplete / 🟠 500/50 not measurable / 🟡 T1 alerts are noisy / 🟡 pg-writer naming / 🟡 no alert on Lua p99)
- All 3 reviews carry a consistent 5.23 04:06-04:07 post timestamp

## Open questions

- Eliaaazzz's response time on the 5 findings in PR #19 (a mentor 5/25 blocker) — their `elia/T1-dummy-bid-roundtrip` branch already has a new commit `0fd3486`, pending audit
- Whether fariZzzz agrees with the V9 canonical Redis key naming (`auction:{<aid>}:state` vs `state:{aid}` in the PR #16 doc)
- Whether PR #16 should rebase onto the PR #19 contract surface before merging
- Whether the demo fallback recording script should be prepared on 5.24 (Sunday)
- Whether the PDGGK Day 0 dev-log sets the benchmark that triggers team adoption

## Next step

- Watch for Eliaaazzz's fix push → fetch + audit + brief (PR #19 commit 0fd3486 pending check)
- Watch for fariZzzz's fixes on PR #16 / #18
- Prepare the 5.24 demo fallback script + recording checklist (the Q9 trio: public + local + recording)
- Monitor PR #13 for a re-review response from Eliaaazzz
- Keep watching cross-PR contract drift consistency
