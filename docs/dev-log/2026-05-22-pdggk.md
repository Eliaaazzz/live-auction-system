# Dev-log — 2026-05-22 — @PDGGK

> Sprint 0 baseline + Workflow v2 RFC day. Reviewer + direction-setting lens.

## What changed

- **PR #13 Sprint 0 baseline** published — 33 files: charter / architecture / state-machine / ws-protocol / redis-keys / mysql-schema / roadmap / decisions (Q1-Q9) / RACI / observed / 7 Mermaid overview diagrams / 6 internal memory notes / AGENTS.md / CLAUDE.md / handoff/templates / .codex-pm.lock
- **Issue #15 RFC posted** — "Workflow v2 → Plan V9 — trunk-driven + dev-log + diverse review (Mon 5.25 mentor sync)"
- Added a **branching convention** proposal in a #15 comment (`<who>/<topic>`, short-lived ≤ 2-3d, delete after merge)
- Fixed 11 review findings on PR #13 → 5 commits: `5d1356f` / `d2305d9` / `768d0d3` / `fde2e75`
- Follow-up `e2b3cd9` for V9 / RFC v2 drift alignment (a minimum fix across 5 files)

## How

- Drafted with AI tooling → consolidated review → self-audit (mmdc parse / isolation grep / V9 numeric-threshold grep) → commit → push → PR
- Issue #15's content combines PDGGK's proposal (async trunk + diverse review + dev-log + one rule set) with a steelman and a Devil's Advocate pass (bus factor / plan-B demo path / the interpersonal cost to fariZzzz)
- The drift fix was kept strictly minimal: only naming and clarification fixes, with **structural changes (reserve / hash chain expansion / proto/) left to the leader**

## AI prompt summary

- "Consolidate V8 + RFC v1 + the PDF spec → land docs/ plus 7 Mermaid overview diagrams" (17 files)
- "Fix 10 PR #13 review findings across 13 files (blocker mmdc + team-memory + 7 high + 3 medium)"
- "Draft the Workflow v2 RFC with a steelman plus a serious Devil's Advocate pass and industry prior art (Linux kernel maintainers / trunk-based development / pair + mob programming / Copilot AI pairing)"
- "PR #13 vs V9 / RFC v2 drift check, minimum fix"

## Decision points

- **Separating internal from public**: CLAUDE.md / AGENTS.md carry the Codex-PM operating model, but `docs/team-memory/` should stay private → Eliaaazzz's review flagged the contamination → `git rm --cached -r` + `.gitignore` (per #13 blocker #2)
- **Adding the mentor 5/25 deadline to the §10 timeline**: pulls the V9 draft deadline forward by a day, for T1 demo readiness at the Monday sync
- **Branching `<who>/<topic>`**: personal prefix + short-lived ≤ 2-3d + delete after merge
- **Reviewer authority is blocking, not advisory**: confirmed by Eliaaazzz's leader-ack in #15

## Tests / evidence

- All 7 diagrams parse under mmdc (the 3 sequence diagrams initially had a `;` syntax error, fixed by `5d1356f`)
- The isolation grep shows the public docs are clean (no `codex-pm` / `codex-supervisor` / `handoff/runs/` strings in CLAUDE.md / AGENTS.md / README.md / the main docs)
- The V9 numeric thresholds are preserved per grep: ack p95 < 80ms / broadcast p95 < 150ms / hammer p95 < 500ms / catchup 200 events < 1s / 500/50 P0 / 1k/100 stretch / WS bufferedAmount 1MB/4MB / AOF everysec

## Open questions

- Q5: PDGGK has not stated a personal time commitment
- The detailed contents of T2-T10 are for Eliaaazzz to define inside V9 (only T0/T1 exist today)
- Dev-log frequency: per commit or per session? To be written as rule 2 once `docs/dev-rules.md` is created

## Next step

- 5.23 Codex adversarial review of PRs #19 / #16 / #18, posted as CHANGES_REQUESTED
- Ping Eliaaazzz for a re-review on PR #13
- Kick off the dev-log discipline (this PR, `pdggk/dev-log`)
- Leave a +1 on #1 V9 / #2 RFC v2 / #11 / #14 (at PDGGK's own discretion)
